export enum DoorValue {
  None = 0,
  Door = 1,
}

export enum MovementValue {
  None = 0,
  Normal = 1,
}

export enum SenseValue {
  None = 0,
  Normal = 1,
  Limited = 10,
  Proximity = 30,
}

export enum SoundValue {
  None = 0,
  Normal = 1,
}
